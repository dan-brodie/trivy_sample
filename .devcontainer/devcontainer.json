{
	"name": "cookiecutter",
	"image": "eu.gcr.io/cts-public-images-1/cts-k8s:latest",

	// Configure tool-specific properties.
	"customizations": {
		// Configure properties specific to VS Code.
		"vscode": {
			// Add the IDs of extensions you want installed when the container is created.
			"extensions": [
				"ms-azuretools.vscode-docker",
				"ms-kubernetes-tools.vscode-kubernetes-tools"
			]
		}
	},
	"remoteUser": "vscode",
	"postCreateCommand": "pre-commit install && wget https://github.com/aquasecurity/trivy/releases/download/v0.43.1/trivy_0.43.1_Linux-ARM64.deb && sudo dpkg -i trivy_0.43.1_Linux-ARM64.deb && rm trivy_0.43.1_Linux-ARM64.deb"
}